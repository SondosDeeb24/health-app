//===========================================================================================================
//?define enum for the appointment status , same as the one used in the database
//===========================================================================================================

export enum appointment_status {
    available= "available",
    booked = "booked"
};

