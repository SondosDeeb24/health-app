//======================================================================
//? appointment status enum
//======================================================================

export enum status{
    available = 'available',
    booked = 'booked'
}