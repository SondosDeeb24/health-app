//======================================================================
//? appointment status enum
//======================================================================

export enum appointment_status{
    available = 'available',
    booked = 'booked'
}