//======================================================================
//? appointment status enum
//======================================================================

export enum ApptStatus{
    available = 'available',
    booked = 'booked'
}